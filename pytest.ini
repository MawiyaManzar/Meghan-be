[pytest]
# Pytest configuration
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Ignore manual test script (it's not a pytest test)
norecursedirs = .git .venv venv __pycache__ *.egg
filterwarnings =
    ignore::DeprecationWarning:pydantic

